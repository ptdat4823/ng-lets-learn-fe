import { joinStrings } from '@shared/helper/string.helper';

export const ROUTES = {
  HOME: '/courses',
  LOGIN: '/auth/login',
  SIGN_UP: '/auth/signup',
  CALENDAR: '/calendar',
  SETTINGS: '/settings',
};
